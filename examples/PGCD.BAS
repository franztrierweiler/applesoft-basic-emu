5  REM EUCLIDEAN GCD ALGORITHM
10 HOME : TEXT
20 LET A = 1071 : LET B = 462
30 IF A < B THEN T = A : A = B : B = T
40 PRINT A, B
50 C = A - INT (A / B) * B
60 A = B : B = C
70 IF B > 0 GOTO 40
80 PRINT "GCD IS "; A
90 END
